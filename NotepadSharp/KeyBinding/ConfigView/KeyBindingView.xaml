<UserControl x:Class="NotepadSharp.KeyBindingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:NotepadSharp"
             mc:Ignorable="d" 
             MinWidth="300"
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>
        <local:BoolToPropValueConverter x:Key="IsEditingBindingColorConverter" ValueForTrue="Red" ValueForFalse="Transparent" />
    </UserControl.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="1.5*" />
            <ColumnDefinition Width="3*" />
        </Grid.ColumnDefinitions>
        <Border Grid.Column="0"
                BorderThickness="1"
                BorderBrush="{Binding IsEditingBinding.Value, Converter={StaticResource IsEditingBindingColorConverter}}">
            <Grid>
                <TextBox TextAlignment="Center" 
                         Background="LightGray" 
                         Text="{Binding Keys.Value, Converter={StaticResource PrettyPrintKeyBindingConverter}, Mode=OneWay}" />
                <!--disable the textbox without using IsEnabled (I don't want the ugly gray)-->
                <TextBox Cursor="Arrow" 
                         IsReadOnly="True" 
                         Background="Transparent"
                         local:CommandBindingBehavior.GotFocusCommand="{Binding StartEditingCommand}" 
                         local:CommandBindingBehavior.LostFocusCommand="{Binding EndEditingCommand}"
                         local:CommandBindingBehavior.PreviewKeyDownCommand="{Binding KeyDownCommand}"
                         local:CommandBindingBehavior.PreviewKeyUpCommand="{Binding KeyUpCommand}"/>
            </Grid>
        </Border>
        <TextBox  Grid.Column="1" TextAlignment="Left" Text="{Binding Label.Value}" />
        <TextBox  Grid.Column="2" TextAlignment="Left" Text="{Binding ScriptFilePath.Value}" />
    </Grid>
</UserControl>
